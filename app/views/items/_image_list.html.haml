.image-list-title
  %h6 Select an Image
  %hr
%div.image-list
  %table
    %tr
      - images.each do |image_url|
        %td
          %img.clickable{ src: "#{image_url}", style: "max-width: 140px;" }